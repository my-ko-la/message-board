// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id                     String    @id @default(cuid())
  username               String    @default("")
  sessionId              String    @unique @default("")
  role                   String    @default("USER")
  messages               Message[] @relation("Message_author")
  createdAt              DateTime? @default(now())
  from_Message_deletedBy Message[] @relation("Message_deletedBy")
}

model Message {
  id                        String    @id @default(cuid())
  content                   String    @default("")
  author                    User?     @relation("Message_author", fields: [authorId], references: [id])
  authorId                  String?   @map("author")
  conversation              Message?  @relation("Message_conversation", fields: [conversationId], references: [id])
  conversationId            String?   @map("conversation")
  parentMessage             Message?  @relation("Message_parentMessage", fields: [parentMessageId], references: [id])
  parentMessageId           String?   @map("parentMessage")
  replies                   Message[] @relation("Message_parentMessage")
  isDeleted                 Boolean   @default(false)
  deletedReason             String    @default("")
  deletedBy                 User?     @relation("Message_deletedBy", fields: [deletedById], references: [id])
  deletedById               String?   @map("deletedBy")
  createdAt                 DateTime? @default(now())
  updatedAt                 DateTime? @updatedAt
  from_Message_conversation Message[] @relation("Message_conversation")

  @@index([authorId])
  @@index([conversationId])
  @@index([parentMessageId])
  @@index([deletedById])
}
